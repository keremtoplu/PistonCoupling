using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MontageController : MonoBehaviour
{
    

    private int currentMontageCount=0;
    public int CurrentMontageCount{ get{return currentMontageCount;} set{currentMontageCount=value;} }
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
